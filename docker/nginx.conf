server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Frontend (React)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy para API (Backend)
    location /api/ {
        # DNS interno do Docker
        resolver 127.0.0.11 valid=30s;
        set $upstream_backend backend;
        
        # Reescreve o caminho para garantir que seja repassado corretamente
        rewrite ^/api/(.*) /api/$1 break;
        
        proxy_pass http://$upstream_backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Repassa o IP real do cliente
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy para Uploads (Imagens)
    location /uploads/ {
        resolver 127.0.0.11 valid=30s;
        set $upstream_backend backend;
        
        rewrite ^/uploads/(.*) /uploads/$1 break;
        
        proxy_pass http://$upstream_backend:3001;
        proxy_set_header Host $host;
    }

    # Cache estático para assets do Vite
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}